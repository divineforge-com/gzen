## 2024-06-28 - Accessibility attributes for language switchers
**Learning:** Missing `hreflang` and `lang` attributes on language switcher links can cause screen readers to apply the wrong pronunciation profile for the target language name.
**Action:** Always add `hreflang` and `lang` matching the target language code to language switcher links to ensure proper screen reader behavior.
