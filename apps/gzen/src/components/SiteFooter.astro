---
import { site } from "../data/paths";
---

<footer class="foot reveal">
  <div class="foot-brand">
    <span class="mark">gZen</span>
    <span class="domain">gzen.io</span>
    <span class="sep" aria-hidden="true">·</span>
    <span class="whisper">{site.originWhisper}</span>
  </div>
  <div class="foot-links">
    <a href="#paths">Portals (Z · E · N)</a>
    <span class="sep" aria-hidden="true">·</span>
    <a href="https://divineforge.com">DivineForge</a>
  </div>
</footer>

<style>
  .foot {
    margin-top: clamp(3rem, 7vw, 5rem);
    padding: 2rem 0 1rem;
    display: flex;
    flex-wrap: wrap;
    gap: 1rem 1.75rem;
    align-items: center;
    justify-content: space-between;
    border-top: 1px solid var(--line);
    font-size: 0.82rem;
    color: var(--dim);
  }

  .foot-brand,
  .foot-links {
    display: inline-flex;
    align-items: center;
    flex-wrap: wrap;
    gap: 0.5rem 0.75rem;
  }

  .mark {
    color: var(--ink);
    font-weight: 550;
    letter-spacing: -0.03em;
    font-size: 0.95rem;
  }

  .domain {
    font-family: var(--font-mono);
    font-size: 0.68rem;
    letter-spacing: 0.04em;
    color: var(--dim);
  }

  .whisper {
    font-family: var(--font-mono);
    font-size: 0.66rem;
    letter-spacing: 0.04em;
    color: var(--dim);
    opacity: 0.85;
  }

  .sep {
    opacity: 0.45;
  }

  a {
    color: var(--muted);
    text-decoration: none;
    border-bottom: 1px solid transparent;
    transition:
      border-color 0.2s var(--ease),
      color 0.2s var(--ease);
  }

  a:hover,
  a:focus-visible {
    color: var(--ink);
    border-bottom-color: var(--accent);
    outline: none;
  }

  @media (max-width: 480px) {
    .foot {
      flex-direction: column;
      align-items: center;
      text-align: center;
      gap: 0.75rem;
      padding: 1.5rem 0 0.75rem;
    }

    .foot-brand,
    .foot-links {
      justify-content: center;
    }
  }
</style>
